language: cpp

dist: xenial
addons:
  apt:
    packages:
      - libasound2-dev
      - yasm

git:
  submodules: false

env:
  global:
    - TOOLCHAIN_ROOT=$TRAVIS_BUILD_DIR
    - TTLIBS_ROOT=$TRAVIS_BUILD_DIR

script:
    - cd build
    - make prepare-github
    - make deb64-github
